'use strict'

/*
1. переменной a присваиваем значение 2
2. в скобках переменной a присваиваем значение 4, в результате умножения (a = a * 2), 2 * 2
3. складываем 1 и 4 получаем 5
4. присваиваем значение 5 переменной x
*/
let a = 2;
let x = 1 + (a *= 2);